import os,sys,math,glob
import DirectoryProc

dp=DirectoryProc.DirectoryProc("./")
dl=dp.getDirList()
ll=dp.findTarget("data")

filename="kamo_override.config"
for eachdir in ll:
    abspath=os.path.join(

ofile=
wavelength= 1
distance= 100
orgx= 2500
orgy= 2500
osc_range= 0.1
rotation_axis= -1 0 0
