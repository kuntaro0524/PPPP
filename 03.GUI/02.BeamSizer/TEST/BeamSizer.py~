#!/usr/bin/env python
# -*- coding: UTF-8 -*-
# generated by wxGlade 0.4.1 on Sat Apr 24 22:09:27 2010

import wx
from SetBeamSize import *

class BeamSizeChanger(wx.Frame):
    def __init__(self, *args, **kwds):
        # begin wxGlade: BeamSizeChanger.__init__
        kwds["style"] = wx.DEFAULT_FRAME_STYLE
        wx.Frame.__init__(self, *args, **kwds)
        self.label_1 = wx.StaticText(self, -1, "Set Beamsize to")
        self.inputSize = wx.TextCtrl(self, -1, "")
        self.label_2 = wx.StaticText(self, -1, "[um] square")
        self.panel_1 = wx.Panel(self, -1)
        self.button_1 = wx.Button(self, -1, "Go")
	
	# my classes
	self.sbs=SetBeamSize()

        self.__set_properties()
        self.__do_layout()

        self.Bind(wx.EVT_BUTTON, self.setButton, self.button_1)
        # end wxGlade

    def __set_properties(self):
        # begin wxGlade: BeamSizeChanger.__set_properties
        self.SetTitle("frame_1")
        # end wxGlade

    def __do_layout(self):
        # begin wxGlade: BeamSizeChanger.__do_layout
        sizer_1 = wx.BoxSizer(wx.VERTICAL)
        sizer_2 = wx.BoxSizer(wx.HORIZONTAL)
        sizer_2.Add(self.label_1, 0, wx.ADJUST_MINSIZE, 0)
        sizer_2.Add(self.inputSize, 0, wx.ADJUST_MINSIZE, 0)
        sizer_2.Add(self.label_2, 0, wx.ADJUST_MINSIZE, 0)
        sizer_2.Add(self.panel_1, 1, wx.EXPAND, 0)
        sizer_2.Add(self.button_1, 0, wx.ADJUST_MINSIZE, 0)
        sizer_1.Add(sizer_2, 1, wx.EXPAND, 0)
        self.SetAutoLayout(True)
        self.SetSizer(sizer_1)
        sizer_1.Fit(self)
        sizer_1.SetSizeHints(self)
        self.Layout()
        # end wxGlade

    def setButton(self, event): # wxGlade: BeamSizeChanger.<event_handler>
        size=float(self.inputSize.GetValue())
	flag=sbs.setSize(size)

	if flag<0:
		print "Error"

# end of class BeamSizeChanger
